import { styled } from "styled-components";

export const WrapperMenuProducts = styled.div`
    margin-bottom: 70px;
    margin-left: 24px;
    font-weight: 600;
    user-select: none;

    .product-main-menu {
        margin-right: 40px;
        cursor: pointer;
        font-size: 17px;
    }

    .active {
        font-weight: 600;
        color: #ff222ec7;
    }
`